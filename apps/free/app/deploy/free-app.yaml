apiVersion: apps/v1
kind: Deployment
metadata:
  name: free-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: free-app
  template:
    metadata:
      labels:
        app: free-app
    spec:
      containers:
      - name: free-app
        image: docker.korshakov.com/free-app:{version}
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
        startupProbe:
          httpGet:
            path: /
            port: 80
          periodSeconds: 10
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: free-app
spec:
  selector:
    app: free-app
  ports:
  - port: 3000
    targetPort: 80
  type: ClusterIP